#ifndef TOTALVAR_H
#include "puzdef.h"
/*
 *   Calculate the total variation for this puzzle for scrambles consisting
 *   of a given number of moves.
 */
void totalvar(const puzdef &pd);
#define TOTALVAR_H
#endif
